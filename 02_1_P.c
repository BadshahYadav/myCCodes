#include <stdio.h>

int main()
{
    int a, b;                    // Per maine define kar dya h b ko taki formet error na de;
    b = a;                       // this is Invalid in C because b is undefined;
    int V = 3 ^ 3;               // This is Valid in C;
    char date = '26 April 2022'; // This is also Valid in C;
    return 0;
}